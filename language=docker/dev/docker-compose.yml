version: '3.8'

services:
  web:
    build: 
      context: ../..
      dockerfile: docker/dev/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../../app:/app
    environment:
      - ENVIRONMENT=development
    restart: always 